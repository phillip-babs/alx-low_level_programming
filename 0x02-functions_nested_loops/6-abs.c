#include "main.h"

/**
 * _abs - compute the absolute value of an integer
 * @n: the integer to be checked
 * Return:  always 0
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
